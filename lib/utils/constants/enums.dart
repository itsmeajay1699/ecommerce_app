enum TextSizes { small, medium, large }

enum OrderStatus { pending, processing, shipped, delivered, cancelled }

enum PaymentMethods {
  paypal,
  creditCard,
  cashOnDelivery,
  googlePay,
  applePay,
  stripe,
  razorPay,
  paytm,
}
